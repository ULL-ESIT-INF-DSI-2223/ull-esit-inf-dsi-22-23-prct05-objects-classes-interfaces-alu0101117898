/* import 'mocha';
import {expect} from 'chai';
import {factorial} from "../src/ejercicio-1";

describe ('--Prueba Coverall--', () => {
  it("factorial(0) returns 1", () => {
    expect(factorial(0)).to.be.equal(1);
  });
}); */