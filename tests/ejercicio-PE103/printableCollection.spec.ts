import 'mocha';
import {expect} from 'chai';
import { NumericPrintableCollection  } from "../../src/ejercicio-PE103/printableCollection";

describe ('--Ejercicio PE103 Pruebas--', () => {
    it("", () => {
      expect().to.be.not.equal(null);
    });
  }); 